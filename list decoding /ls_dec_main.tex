\include{preamble}
\begin{document}
	
	\pagestyle{empty}
	\include{titlepage}
	
		\vspace*{\fill}
		\begin{abstract}
			
		\noindent In the Hamming setup, we have seen that any code $\calc$ over the alphabet $\Sigma$ with distance $d$ can correct upto $\lfloor\frac{d-1}{2}\rfloor$ errors. The main goal of list decoding is to get past this bound. We shall also see that list decoding is, to an extent, the bridge between Hamming and Shannon through its capacity.

			\vspace*{\fill}

		%% writing left %%% 
	\end{abstract}
	
	\newpage
	
	\tableofcontents	
	
	\newpage
	
	\section*{Notations}
	\addcontentsline{toc}{section}{\large Notations}
	
	\noindent We denote a finite set of symbols, called an \emph{alphabet}, by $\Sigma$. A code $\calc$ of block length $n$ over the alphabet $\Sigma$ is just a subset of $\Sigma^n$.
	\vspace{0.1in}
	
	\noindent For $n\in\mathbb{N} := \{1,2,\ldots\}$, we define $[n]$ to be the set $\{1,2,\ldots,n\}$.
	\vspace{0.1in}
	
	\noindent We denote the $q$-entropy function by $H_q(\rho) := \rho \log_q(q-1) - plog_qp - (1-p)log_q(1-p)$.
	\vspace{0.1in}
	
	\noindent Let $q$ be a prime power. Then $\mathbb{F}_q$ denotes the finite field of order $q$.
	\vspace{0.1in}
	
	\noindent We denote the Hamming distance by $\Delta(\bfx,\bfy) := \#\{i\in[n] | x_i \neq y_i\}$ for $\bfx, \bfy\in\Sigma^n$, and the Hamming Ball by $B(\bfy,e):=\{x\in\Sigma^n | \Delta(\bfx,\bfy)\leq e\}$.
	
	
	\newpage
	
	\pagestyle{fancy}
	
	\newpage
	
	\pagenumbering{arabic}
	
	\include{list_decoding}
	
	\newpage
	
	\include{rs_decode}
	
	
	\begin{thebibliography}{}
		
		\bibitem{guruswami}
		V.~Guruswami, A.~Rudra, and M.~Sudan, \textsf{Essential Coding Theory}, 
		Graduate Studies in Mathematics, vol.~226, American Mathematical Society, 2024.
	\end{thebibliography}
	
\end{document}
